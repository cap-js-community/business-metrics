{
  "name": "@cap-js-community/business-metrics",
  "version": "1.0.0",
  "description": "Counter and Guage metrics for @cap-js/telemetry",
  "main": "cds-plugin.js",
  "files": [
    "lib",
    "cds-plugin.js"
  ], 
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cap-js-community/business-metrics.git"
  }, 
  "license": "Apache-2.0",
  "dependencies": {
    "@opentelemetry/api": "^1.9.0"
  },
  "peerDependencies": {
    "@sap/cds": ">=9",
    "@cap-js/telemetry": "^1.6.0"
  },
  "devDependencies": {
    "@cap-js/cds-test": "^1.0.1",
    "@cap-js/cds-types": "^0.18.0",
    "@cap-js/sqlite": "^2.4.0",
    "business-metrics": "file:.",
    "eslint": "^9.23.0",
    "jest": "^30.0.5"
  },
  "scripts": {
    "start": "cds-serve",
    "lint": "npx eslint .",
    "test": "npx jest --maxWorkers=1 --silent --detectOpenHandles --forceExit",
    "test-file": "npx jest test/counter-metrics.test.js --silent",
    "coverage": "npx jest --coverage --maxWorkers=1 --silent --detectOpenHandles"
  }
}
